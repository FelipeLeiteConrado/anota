spring:
  datasource:
    url: jdbc:postgresql://ep-dark-rain-ack0aix2-pooler.sa-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require
    username: neondb_owner
    password: npg_UnrwjI7LN5AW
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  flyway:
    enabled: true
    locations: classpath:db/migration

jwt:
  secret: sua-chave-secreta-longa-e-aleatoria-aqui-minimo-32-caracteres

server:
  port: 8080